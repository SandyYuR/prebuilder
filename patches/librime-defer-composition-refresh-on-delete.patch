diff --git a/src/rime/context.cc b/src/rime/context.cc
index 07171f34..a21b6bc1 100644
--- a/src/rime/context.cc
+++ b/src/rime/context.cc
@@ -175,7 +175,16 @@ bool Context::DeleteCandidate(size_t index) {
   Segment& seg(composition_.back());
   seg.selected_index = index;
   DLOG(INFO) << "Deleting candidate: " << seg.GetSelectedCandidate()->text();
+  composition_refresh_pending_ = false;
   delete_notifier_(this);
+  // Rebuild the composition only after every listener has observed the same
+  // (pre-refresh) state. Refreshing inside a listener re-translated the
+  // composition mid-dispatch, so the remaining listeners read a *different*
+  // candidate and deleted a second, unrelated word.
+  if (composition_refresh_pending_) {
+    composition_refresh_pending_ = false;
+    RefreshNonConfirmedComposition();
+  }
   return true;  // CAVEAT: this doesn't mean anything is deleted for sure
 }
 
@@ -278,6 +287,10 @@ bool Context::ClearNonConfirmedComposition() {
   return reverted;
 }
 
+void Context::requestCompositionRefresh() {
+  composition_refresh_pending_ = true;
+}
+
 bool Context::RefreshNonConfirmedComposition() {
   if (ClearNonConfirmedComposition()) {
     update_notifier_(this);
diff --git a/src/rime/context.h b/src/rime/context.h
index 79588d58..ab231e4f 100644
--- a/src/rime/context.h
+++ b/src/rime/context.h
@@ -61,6 +61,11 @@ class RIME_DLL Context {
   bool ReopenPreviousSelection();
   bool ClearNonConfirmedComposition();
   bool RefreshNonConfirmedComposition();
+  // Request a rebuild of the non-confirmed composition to be performed once
+  // the current delete_notifier dispatch has finished. Refreshing in the
+  // middle of the dispatch let later listeners observe a different (already
+  // re-translated) candidate and delete a second, unrelated word.
+  void requestCompositionRefresh();
 
   void set_input(const string& value);
   const string& input() const { return input_; }
@@ -128,6 +133,7 @@ class RIME_DLL Context {
   map<string, string> properties_;
   vector<size_t> tab_cursors_;
   vector<TabConstraint> tab_constraints_;
+  bool composition_refresh_pending_ = false;
 
   Notifier commit_notifier_;
   Notifier select_notifier_;
diff --git a/src/rime/gear/memory.cc b/src/rime/gear/memory.cc
index 9da80fa9..ef9e2909 100644
--- a/src/rime/gear/memory.cc
+++ b/src/rime/gear/memory.cc
@@ -144,7 +144,13 @@ void Memory::OnDeleteEntry(Context* ctx) {
     const DictEntry& entry(phrase->entry());
     LOG(INFO) << "deleting entry: '" << entry.text << "'.";
     user_dict_->UpdateEntry(entry, -1);  // mark as deleted in user dict
-    ctx->RefreshNonConfirmedComposition();
+    // Do not rebuild the composition here: delete_notifier_ is a multicast
+    // signal and several translators (each owning a user dictionary) listen to
+    // it. Refreshing mid-dispatch re-translated the composition, so the
+    // remaining listeners read a different candidate and deleted a second,
+    // unrelated word ("forget one word, lose two"). Ask for one rebuild after
+    // the whole dispatch instead.
+    ctx->requestCompositionRefresh();
   }
 }
 
